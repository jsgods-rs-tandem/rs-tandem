# @rs-tandem/shared

Общий TypeScript-пакет для RS-Tandem. Содержит только интерфейсы и типы, используемые одновременно во frontend и backend. Никакой runtime-логики нет.

## Использование

```ts
import type { UserDto, LoginDto, RegisterDto, AuthResponseDto } from '@rs-tandem/shared';
```
